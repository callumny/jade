#!/bin/bash

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"

# Activate the virtual environment and run the jade command
source "$SCRIPT_DIR/.venv/bin/activate" && jade "$@"